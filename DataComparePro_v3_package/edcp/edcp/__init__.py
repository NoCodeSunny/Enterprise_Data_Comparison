# -*- coding: utf-8 -*-
"""
DataComparePro (edcp) — Enterprise Data Comparison Platform v3.0.0
===================================================================
Canonical package: edcp.*
  edcp.batch.*      — BatchManager, QueueController, RecoveryManager, EngineSelector
  edcp.validation.* — PreFlightValidator
  edcp.audit.*      — EnterpriseAuditLogger
  edcp.jobs.*       — ComparisonJob
  edcp.loaders.*    — file_loader, encoding

data_compare.* remains as a thin compatibility layer backed by edcp.
"""
__version__      = "3.0.0"
__product__      = "DataComparePro"
__package_name__ = "edcp"
